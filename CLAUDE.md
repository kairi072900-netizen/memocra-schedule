# CLAUDE.md — メモクラ スケジュール管理アプリ

このファイルは開発ルールの唯一の参照先。作業前に必ず読むこと。
仕様の出典は `メモクラ_スケジュール管理アプリ_要件定義書_v0.4.md`。
このファイルと要件定義書が食い違った場合は**要件定義書が正**。気づいたらこのファイルを直す。

---

## 1. このアプリの目的（要件定義書 第2章）

**動画1本あたりの工程を自動で分解し、担当と締切を撮影当日に確定させることで、リーダーの抱え込みを防ぎ、制作を止めない。**

グループYouTuber「メモリークラフト」（4名）のためのツール。単なる予定表ではなく、
**動画1本ごとの制作パイプラインを管理するツール**として設計する。

成功指標（リリース3ヶ月後に測る）:

| 指標 | 目標 |
|---|---|
| 担当者未定のまま24時間以上放置されるタスク数 | 0件/週 |
| リーダーの担当タスク比率 | 60%以下 |
| 公開予定日に対する遅延率 | 20%以下 |
| 金曜会議の所要時間 | 現状比30%短縮 |

判断に迷ったら「この変更は上の4つのどれかを動かすか？」で決める。

---

## 2. 開発フェーズと現在地

### 現在地: **ユーザー要望の7項目を実装・デプロイ完了 → 次は P3（4人に配る）／本番動作確認**

**2026-08-30〜31 に、要件定義書の外側の要望7件を実装した**（下記「要望7項目」）。
P9（外部カレンダー）と P10（AI）の Edge Function は **2026-08-31 に本番へデプロイ済み**。

- CLI は `npm install --save-dev supabase` で導入（Homebrew 不使用の環境）。`npx supabase` で使う
- Supabase プロジェクト Ref: `heqkeflguxahvrmfunvg`
- secrets: `GEMINI_API_KEY` 設定済み。モデルは `gemini-3.6-flash`
  （`gemini-2.0-flash` は Google が廃止済み。さらに変わったら
   `npx supabase secrets set GEMINI_MODEL=...` で追従、再デプロイ不要）
- `ai` の「今週のまとめ」（`responseSchema` 使用）が本番で成功を確認

デプロイ手順の全文と、モデル差し替え・トークン再発行は `supabase/functions/README.md`。

**P1でやること: Supabase接続 + 認証 + RLS。**
完了条件は **「4人がログインでき、予定が永続化される」** こと。

進捗（2026-08-29）: Supabase 接続・**Google ログイン往復・合言葉によるメンバー登録・RLS 有効化**
まで本番（`https://memocra-schedule.vercel.app/`）で完了。古い Vercel プロジェクト
`memocra` は削除済み。コード面の P1 は完了。

**方針変更（2026-08-29）: P3（4人への配布・試用開始）を後ろにずらす。**
当初は「早めに配ってフィードバックを見る」だったが、ユーザーの判断で
**「P2（出欠）+ P5（タスク）くらいまで作り込んでから配る」** に変更。
理想像はモックアップ（`メモクラ_..._要件定義書_v0.4.md` 添付のフルUI）。ただし段階的に
近づける方針は不変（一気に全部作らない。§2「P0のスコープ外」/ 要件定義書 12.8）。
**Lv / HP（ゲーミフィケーション）はモックアップにあるが引き続き作らない**
（§2。HPを負荷指標にしない）。次の実装は **P2 → P5**。

P0（ダミーデータでカレンダーUI）は完了。ドット絵フォントの実機確認だけ Expo Go 待ち（§7.1）
だが、web 先行方針のため P1/P3 のブロッカーではない。

| フェーズ | 内容 | 状態 |
|---|---|---|
| P0 | 画面設計・ダミーデータでカレンダーUI（ローカル状態） | 完了（実機確認のみ保留） |
| P1 | Supabase接続 + 認証 + RLS（members / projects / streams） | コード面 完了 |
| P2 | 配信の出欠機能（availabilities）+ 配信の登録UI | 実装完了 |
| P3 | **web アプリを Vercel に配布・メモクラで試用開始**。web デプロイ自体は P1 で完了済み
       （`memocra-schedule.vercel.app`）。**実際の配布は P2+P5 を作り込んでから**（2026-08-29 方針変更）。
       当面 web 先行、ネイティブ配布は将来。§7.1 / §5.3 | |
| P4 | プッシュ通知（出欠依頼・リマインド） | **P5より後ろに回した**（下記） |
| P5 | タスク管理 + テンプレート自動生成 + 締切逆算 | 実装完了・本番確認済み |
| **P6** | **負荷ダッシュボード** | **実装完了・動作確認待ち** |
| P7 | 会議モード・コメントスレッド | 議事録だけ P10 で先行実装 |
| P8 | マルチチーム対応（汎用化） | |
| **P9** | **外部カレンダー取り込み（Google / TimeTree の ICS）** | **デプロイ済み・本番動作確認待ち** |
| **P10** | **専用AI（Gemini）5用途** | **デプロイ済み・「今週のまとめ」は本番成功** |

### P1の進捗

- [x] `@supabase/supabase-js` の導入と `src/lib/supabase.ts`（`.env` から接続情報を読む）
- [x] `supabase/migrations/0001_init.sql`（6テーブル・トリガー・暫定ポリシー）
- [x] SQLをSupabaseのSQL Editorで実行する
- [x] `src/lib/api.ts` を新設し、`getProjects()` をSupabase接続 + asyncに切り替え（§5.2）
      呼び出し側（S0）に loading / error 表示を追加（`src/components/async-state.tsx`）
- [x] `getStreams()` も同様にSupabase接続 + asyncに切り替え
      S0のエラー表示は projects / streams をまとめて1枚のカードにしている
- [x] `getAvailabilities()` も同様にSupabase接続 + asyncに切り替え
      エラーカードは projects / streams / availabilities の3つをまとめている
- [x] `getMembers()` も同様にSupabase接続 + asyncに切り替え（4種類すべて完了）
      エラーカードは4つ全部をまとめている
- [x] `src/data/dummy.ts` を削除し `src/data/` ごと片付けた
- [x] 認証（**Googleログインのみ**。要件定義書のメールOTPから変更。§5.3参照）と
      セッション永続化（AsyncStorage、ただしwebはSSR対応のため既定実装に委ねる）
- [x] OAuth同意画面を**本番公開**（2026-08-28）。公開に必須のホームページ／プライバシー
      ポリシーURLは **GitHub Pages（`docs/`）** で用意・公開済み（§5.3）
      `https://kairi072900-netizen.github.io/memocra-schedule/`。ブランディング入力・本番切替も完了
- [x] **web 先行の方針に変更**（2026-08-28、§7.1 / §5.3）。`app.json` の web 出力を
      `static` → `single`（SPA）。`vercel.json` を追加。
- [x] **web の Google ログイン往復が本番で成立**（2026-08-29）。`src/app/login-callback.tsx`
      が戻り URL の `?code=` を `exchangeCodeForSession` で自前交換（`detectSessionInUrl` は
      web/native とも false）。`login.tsx` は Platform 分岐（web=フルページ遷移）。§5.3
- [x] `supabase/migrations/0002_auth.sql` を SQL Editor で適用（`claim_membership` / `team_passcode`）。
      合言葉は現在 `0807`（`team_passcode` テーブル・平文。§5.3）
- [x] `src/lib/auth.tsx`（旧 `auth.ts`）を `SessionProvider`（Context）化。合言葉入力後に
      リロード無しでカレンダーへ遷移するようになった（§5.3）
- [x] **Vercel 本番稼働: `https://memocra-schedule.vercel.app/`**（プロジェクト名 `memocra-schedule`、
      team `memocra`、Hobby）。GitHub push で自動デプロイ。`.env` の2値を環境変数に登録済み。
      Supabase の Redirect URLs に `https://memocra-schedule.vercel.app/login-callback` と
      `http://localhost:8081/login-callback`、Site URL も同ドメイン。
      Google ログイン → 合言葉 → カレンダーまで PC ブラウザで到達確認済み
- [ ] 古い Vercel プロジェクト `memocra`（`memocra.vercel.app`）の削除（誤って2つ作った。stale）
- [ ] `members` テーブルの表示名・役割・識別色の手直し（§5.3。メンバー参加の都度）
- [x] **RLS の有効化と正式なポリシー完了**（2026-08-29、`supabase/migrations/0003_rls.sql`）。
      6テーブルで RLS enable、`current_team_id()`（security definer）で team_id 一致を判定、
      `temporary_full_access` を撤去。SQL Editor で適用済み。
      確認: ログイン中の自分はカレンダー表示OK / anon の REST 直叩きは全テーブル `[]`
- [ ] 他の3人のログイン実確認（合言葉 `0807` を配布。**P2+P5 の作り込み後**。§2）

### P2の進捗

**P2の完了条件: 「配信を登録 → 全員が回答 → カレンダーで一覧できる」**（要件定義書）。

- [x] `src/lib/api.ts` に書き込み関数（`createStream` / `updateStream` / `deleteStream` /
      `setAvailability` / `clearAvailability`）。`created_by`・`member_id` は
      `getSession()` のユーザーで固定。`availabilities.team_id` はトリガー任せで送らない
- [x] `src/components/stream-form.tsx`（作成・編集で共用）。日時は専用ピッカーを作らず
      書式ヒント付きテキスト入力（`YYYY-MM-DD` / `HH:MM`）。**後の仕上げフェーズで差し替える**。
      フォーム下にメンバーの `active_hours` を読み取り専用表示（要件定義書 F7）
- [x] `src/app/(tabs)/new.tsx`（＋タブ＝配信登録）→ 登録後は詳細へ。企画の登録は P5
- [x] `src/app/stream/[id].tsx`（詳細・自分の回答・編集・削除）。
      `_layout.tsx` の `isMember` グループに明示登録（未宣言ルートは保護対象外になるため）
- [x] `src/app/(tabs)/availability.tsx`（これからの配信一覧 + その場で ○△✕ 回答）
- [x] `src/components/event-card.tsx` に `onPress`、`calendar.tsx` から配信詳細への導線。
      両タブとも `useFocusEffect` で復帰時に再取得
- [x] 配信詳細の「もどる」が効かない問題を修正（`router.replace` で来るとスタックが空に
      なるため、`canGoBack()` を見て履歴が無ければ `/calendar` へ）
- [x] **モックアップに寄せた UI 改修**（2026-08-29。要望「デザインを近づけて欲しい」）
      - `react-native-svg` を導入し `src/components/pixel/icon.tsx` で
        **ドット絵アイコンをコードで描く**（画像を持たない。§3.1「矩形として描く」）。
        予定種別4種（配信はマイク。クリーパー等は使わない §3.3）、タブバー5種、
        メンバーアバター（髪・服は `Member.color`、肌は共通色 §3.2）
      - 下部タブバーにアイコン。カレンダーのヘッダーに `‹ ›`・新規登録ボタン・メンバー行
      - 凡例 `calendar-legend.tsx`（種別4＋出欠4で固定 §3.4）。**チップ表示の幅のときだけ出す**
      - セルの予定を `schedule-chip.tsx`（色帯＋アイコン＋時刻＋タイトル、配信はミニアバターと
        集約バッジ）に。チップ表示のときだけセルを高くする
      - `schedule-summary.tsx`（今日の予定／今週の公開予定）をカレンダー下に
- [ ] **ユーザーによる動作確認**（配信を登録 → 回答 → カレンダー反映 → 編集 → 削除、
      および新しい見た目。**要スーパーリロード `Cmd+Shift+R`**）
- [ ] 出欠依頼・リマインドの通知は **P4**（要件定義書 F7 の後半）
- [ ] PCの左サイドバー・3カラム、背景の風景ドット絵、週表示は**後回し**
      （タスク一覧・負荷ダッシュボードの中身が揃ってから。手戻りを避ける）

### P5の進捗

**P5の完了条件: 「撮影終了→ボタン1つで11タスク生成」**（要件定義書）。
要件定義書が「本アプリの中核機能」と呼んでいる部分で、§1の成功指標はこれが無いと動かない。

- [x] `src/lib/task-template.ts`: 種別ごとのテンプレート（ロング11/ショート4/SNS2/その他2工程）、
      `buildTasksFromTemplate`（公開日からの日数逆算）、`pastDueTasks`（F2の警告）。
      **標準担当は `Member.role` で参照**し、一致する人が居なければ未割当のままにする
      （実名をコードに書かない §3.2。未割当が見えないと成功指標を測れない §1）
- [x] `src/lib/project-status.ts`: `deriveProjectStatus`（タスクの進捗から状態を導く。F3）。
      **公開日より後の工程**（ショート切り出し・投稿後分析）を手前の判定に混ぜない
      （混ぜると投稿済みなのに「撮影済」に戻る）。`myOpenTasks` / `unassignedTasks` / `daysUntil`
- [x] `src/lib/api.ts`: `getTasks` と projects / tasks の CRUD（`createTasks` は bulk insert）
- [x] `src/components/project-form.tsx` + `(tabs)/new.tsx` を「企画／配信予定」の2択に。
      種別を選ぶとテンプレート名と工程数が出る（F1）。既定は企画
- [x] `src/components/task-row.tsx`: 担当とステータスをその場で変更。
      **ブロック中は理由が必須**で空のままでは変更させない（F3）
- [x] `src/app/project/[id].tsx`（S3）: 導出ステータス・未割当件数・工程一覧・編集・削除
      （工程が消えることを件数付きで警告。§5.5と同じ作法）
- [x] `src/app/(tabs)/projects.tsx`（S2）: ステータス別のリスト。かんばんにしないのは
      モバイルで横スクロールが要らないため
- [x] カレンダー下に「締切タスク（自分）」パネル
- [ ] **ユーザーによる動作確認**（企画を作る → 11工程が並ぶ → 担当/ステータス変更 →
      カレンダーに締切が出る。**要 `Cmd+Shift+R`**）
- [ ] **`members.role` の入力**。空だと全工程が未割当になる（仕様どおりだが標準担当が効かない）。
      Supabase の Table Editor で「企画」「建築」「サムネ」「ショート編集」を入れると効く

### P6の進捗

**P6の完了条件: 「偏りが色で分かる」**（要件定義書）。**§1の成功指標を直接見る画面。**

- [x] `src/lib/workload.ts`: `buildWorkloads`（未完了タスク数・7日以内の締切数・割合）、
      `overloadedMembers`（F4「1人に50%以上」）、`unassignedCount`。
      **誰がリーダーかはコードに書かない**（条件は「誰であれ50%以上」なので不要。§3.2）。
      **割合の分母に未割当を入れない**（未割当が増えるほど各人の割合が下がって警告が
      消えるため。逆効果になる）。担当が1人しかいないうちは警告しない（必ず100%になり
      警告が無視されるようになるため）。締切は超過も7日以内に数える
- [x] `src/components/workload-summary.tsx`: 横棒グラフ。ライブラリを使わず硬い矩形（§3.1）。
      **色だけで警告せず** ⚠ と「全体の60%」の言葉を必ず併記（§3.4）
- [x] `src/app/workload.tsx`（S5）: 負荷グラフ＋担当未定の工程一覧＋各メンバーの内訳。
      タブは増やさず、カレンダー下のパネルの「すべて見る ›」から遷移
- [x] カレンダー下に「負荷サマリー」パネル（モックアップ下段の3枚目）
- [ ] **ユーザーによる動作確認**（工程の担当を1人に寄せる → 警告が出る。**要 `Cmd+Shift+R`**）

### UI（モックアップの完成形へ）の進捗

要件定義書 12.7 のモックアップに寄せる作業（2026-08-30）。
P5・P6 でタスク一覧と負荷ダッシュボードの中身が埋まり、サイドバーを作る条件が揃った。

- [x] **PCで左サイドバー、モバイルで下タブ**（`src/components/nav/`）。
      同梱 bottom-tabs の `tabBarPosition: 'left'` に乗せ、自前でレイアウトを組み替えない。
      境界は `LAYOUT.sidebarMinWidth`（900px）
- [x] サイドバー10項目／下タブ5項目。**下タブに出ない画面へは画面内リンクから辿る**
      （約束は `src/components/nav/items.tsx` のコメントに明記）
- [x] 加点型 Lv と EXPバーをサイドバー下部に（HPは無し）
- [x] 新規4画面: `home`（S1 今週）/ `tasks`（タスク一覧）/ `members`（S6）/ `settings`
- [x] カレンダーのヘッダー（週表示トグル・ベル・設定）と最下部の凡例バー
- [x] **バグ修正**: カレンダーのセル幅を `useWindowDimensions` から `onLayout` の実測に変更。
      ウィンドウ幅で計算していたため、サイドバーが出ると**土曜の列が切れて**いた
- [ ] **ユーザーによる動作確認**（PCでサイドバー、iPhoneで下タブ。**要 `Cmd+Shift+R`**）
- [ ] 背景の風景ドット絵（空・草原・城）は**今回やらない**。レイアウトを固めてから
- [ ] 週表示の中身は F6 で後日（トグルだけ置いた）

### 要望7項目の進捗（2026-08-30）

要件定義書の外側でユーザーから出た7件。**短期 / 中期 / 中長期の3段階**に切って進めた
（3番目の「短期目標・中長期の目標」が、この切り方そのものと、アプリの目標機能の両方を指していたため）。

#### 短期（外部サービスの設定を1つも増やさない範囲。完了）

- [x] **日付タップから登録**（要望6・使いやすさ）。カレンダーのセル長押しと、
      選択日パネルの「＋ 追加」から `/new?date=...` へ。**開くだけで登録はしない**
- [x] **日時ピッカー**（使いやすさ）。`components/pixel/date-picker.tsx`。
      `buildMonthGrid` を再利用した自前の月グリッドと、時＋15分刻みのボタン。
      **OSネイティブのピッカーは使わない**（web で角丸が混入する。§3.1）。手打ちも残す
- [x] **押せるものを最低44px に**（使いやすさ）。`LAYOUT.minTapSize`。
      **ドット絵フォントは大きくできない**（17の整数倍のみ）ので、当たり判定と余白で解決した
- [x] **ロング編集を5工程に分割**（要望4）。カット(-6)/テロップ(-5)/SE(-4)/BGM(-4)/
      エフェクト(-3)。ロング動画テンプレートは 11 → 15工程。
      **`TaskKind` は `edit_long` のまま**（§3.2。`tasks.kind` の CHECK 制約も触らない）。
      初期担当は5つとも「企画」ロール＝今の実態。偏りをそのまま負荷グラフに出すため
- [x] **背景の風景ドット絵**（要望1）。`components/pixel/scenery.tsx`。
      置いたのは §3.1 が許す場所だけ（新規登録のヘッダー帯／空状態／ログイン画面）。
      **リスト領域とカレンダーのヘッダーには置かない**
- [ ] **ユーザーによる動作確認**（**要 `Cmd+Shift+R`**）

#### 中期（SQL 手貼りの範囲。完了）

- [x] **目標機能**（要望3のアプリ側）。`0004_goals.sql` / `lib/goal.ts` /
      `components/goal-card.tsx` / `(tabs)/goals.tsx`。
      チーム・個人・企画の3スコープ × 短期・中長期の2軸
- [x] **`0004_goals.sql` を SQL Editor で実行**（2026-08-31 適用済み）

#### 中長期（Edge Function。2026-08-31 デプロイ完了）

- [x] **Edge Function の土台**。`supabase/functions/_shared/auth.ts`。
      **service_role は使わず**、呼び出し元の JWT で RLS を効かせる（§3.5）
- [x] **外部カレンダー取り込み**（要望5）。`sync-ics` + `0005_external_calendars.sql`。
      Google / TimeTree の公開 ICS URL を読み取り専用で取り込む。
      ブラウザから直接 fetch すると CORS で弾かれるのでサーバー経由が必須
- [x] **専用AI**（要望7）。`ai` + `0006_meetings.sql` + `lib/ai.ts`。
      自然文で登録 / 進捗まとめ / 壁打ち / 工程調整 / 議事録の5用途
- [x] **`0005` / `0006` を SQL Editor で実行**（2026-08-31 適用済み。
      Storage バケット `meeting-audio` も作成済み）
- [x] **CLI 導入・`supabase link`・`GEMINI_API_KEY` の secret 設定・
      `functions deploy sync-ics` / `ai`**（2026-08-31 完了）
- [x] **`invoke` のエラーが「non-2xx」しか出ず原因が分からない問題を修正**。
      `FunctionsHttpError.context`（Response）のボディから実際の文言を取る
      （`lib/ai.ts` / `lib/api.ts`）。これで `gemini-2.0-flash` 廃止の 404 に気づけた
- [ ] **無料枠の使用量を運用しながら見る**（私には断定できない）。Supabase Free の
      Edge Functions 上限、Gemini の無料枠、音声入力が無料枠の対象か。
      使用量は Supabase ダッシュボードと Google AI Studio で確認
- [ ] **他のAI用途の本番確認**（自然文登録・議事録・工程調整。`Cmd+Shift+R`）

#### この一連で決めた、破ってはいけない約束

**AIの出力でDBを自動更新しない。**
- 自然文の読み取り結果は**フォームを埋めるだけ**。登録は人がボタンを押す
- 工程の調整案は**差分を並べてから**「適用」で反映する
- 議事録の ToDo は人が選んだものだけ工程にし、**担当は未割当のまま**にする
- 催促の文面は下書きまで。送信はしない（アプリ内DMは作らない。要件定義書 F5）

理由は §1。担当と締切を人が撮影当日に確定させることがこのアプリの目的そのもので、
AI が推測で埋めると「誰も決めていない」ことが見えなくなり、成功指標が測れなくなる。

**外部予定で予定種別（`SCHEDULE_KIND`）を4種類から増やさない。**
5色目を足すと凡例が4→5になり、「凡例を画面ごとに変えない」（§3.4）と
1画面に収める制約（§3.1）の両方に響く。外部予定は `ScheduleEvent.source` で
区別し、灰色＋「外」の文字で示す。凡例には区切り線の外側に1項目だけ足す。

**`lib/level.ts` に減る要素を足さない**（既存の約束。目標機能でも守った）。
Lv は完了タスク数だけで増える加点型。目標には未達がありうるので、混ぜると
却下したHP案に戻る。

### 成功指標をどこで見るか（§1との対応）

| 指標 | 見る場所 |
|---|---|
| 担当者未定のまま24時間以上放置されるタスク数 | 負荷ダッシュボードの「担当が未定の工程」／プロジェクト一覧の件数 |
| リーダーの担当タスク比率 | 負荷ダッシュボードの割合（50%以上で警告） |
| 公開予定日に対する遅延率 | 工程一覧の「N日超過」／締切タスク（自分） |
| 金曜会議の所要時間 | P7 の会議モードで測る（未実装） |

**「24時間以上放置」の自動検知は P4（通知）が要る。** 今は画面を見に行けば分かる状態。

### P4を後ろに回した理由（2026-08-29）

web 先行（§7.1）なので Expo の push（native向け）が使えず、Web Push には
Service Worker + VAPID鍵 + 配信バックエンドが要る。加えて通知の中身
（締切リマインド・未割当の放置検知）は**タスクがあってはじめて意味を持つ**ので、
P5 の後に作るほうが手戻りが少ない。**P5 を先に完成させた。**

### P0の進捗

- [x] Expoプロジェクト作成（Expo Router / TypeScript）
- [x] `src/types/index.ts`（データモデル）
- [x] `src/constants/theme.ts`（色・余白・フォントサイズ）
- [x] DotGothic16の読み込みと `src/components/app-text.tsx`（既定フォントの適用）
- [x] `src/data/dummy.ts`（ダミーデータ）
- [x] `src/components/pixel/frame.tsx`（木枠。四隅の金具＋辺のborderWidth）
- [x] S0 月カレンダー画面（月表示・前月/次月/今日・予定の色分け・出欠バッジ）
- [x] 選択日の予定リスト（予定カード・配信の個人出欠）
- [x] 下部タブバーの骨組み（5タブ。カレンダー以外は「準備中」）
- [ ] **ドット絵フォントの実機確認**（Expo GoがSDK 57に対応したら実施。§7.1参照）

### P0のスコープ外（作らない）

モックアップは**全機能が実装された最終形**を描いたもの。P0で作るのは月カレンダーと予定ドットだけ。
負荷ダッシュボード・タスク一覧・お知らせ履歴・3カラムレイアウト・会議モードは**後のフェーズで足す**。
最初から全部を再現しようとしないこと（要件定義書 12.8）。

ゲーミフィケーション: **Lv（加点型）は実装した。HP は引き続き作らない**（2026-08-30 方針変更）。
要件定義書 12.6 の「採用するなら**加点方向に倒す**。減る指標は置かない」に沿い、
`src/lib/level.ts` で**完了タスク数だけ**からレベルを決めている。
**HPを負荷の指標にしてはいけない**（負荷集中は個人の問題ではなく仕組みの問題のため）という
判断は変えていない。負荷の可視化は負荷サマリー（`src/lib/workload.ts`）が担当する。
**`level.ts` に減る要素を足さないこと。** 未完了数・遅延・負荷の偏りを反映させると、
却下したはずのHP案に戻る。

---

## 3. 【重要な制約】

破ると手戻りが大きい順に並べてある。実装前・レビュー時にこの5つを毎回確認すること。

### 3.1 UIはRPG風ドット絵。角丸（borderRadius）を一切使わない

- **`borderRadius` は使用禁止。** 例外なし。ピクセルの直角を活かす。
- 影は `shadowRadius` によるぼかしではなく、**1〜2pxの硬い矩形**（オフセットした濃色のView）で表現する。
- 枠線は角丸なしの `borderWidth` ＋ 濃淡2色の疑似的な立体枠で作る。
- 装飾フレーム（木枠）は9-sliceを使わない。**四隅の金具だけを `position: absolute` の小さな画像で置き、辺は単色の `borderWidth`** で済ませる。これで見た目の9割が再現でき、実装コストは1/5になる。
- ドット絵フォントは**指定サイズの整数倍でのみ使う**（**17 / 34 / 51px**）。中途半端な倍率はにじむ。
  要件定義書 12.4 の例示は「12px, 24px…」だが、これはPixelMplus（12px基準）を想定したもの。
  採用した **DotGothic16 の実測上の基準は17px** だったため、その整数倍を使う。
  （`unitsPerEm` が1000で1グリッド62.5unitsが整数に丸められており、16の倍数では揃わない。
  日本語4サンプル × デバイスピクセル比1/2/3で実測した結果は `src/constants/theme.ts` のコメント参照）
  本文が長い箇所（メモ、議事録、ブロック理由）は可読性優先で通常のゴシックに切り替えてよい。
  その場合は `LONG_TEXT`（ゴシック15px）を使う。ドット絵フォントではないのでグリッドの制約は無い。
- **例外: 下部タブバーのラベルだけ `TAB_LABEL`（ゴシック10px）を使う。**
  ドット絵17pxだと「プロジェクト」（全角6文字＝102px）がタブ1つぶんの幅に収まらない。
  幅が足りないのは、375pt端末でタブ1つが75pxしか無いうえ、React Navigationが
  タブボタンの内側に5pxのパディングを持っており（`tabBarItemStyle` では上書きできない）
  実際に使えるのが65pxしかないため。ラベル文言を短くするより可読性を優先してこの例外を置いた。
  **ドット絵アイコンを追加してラベルの必要幅が変わったら、この例外を見直すこと。**
  アイコンで意味が伝わるならラベルを短くでき、17pxのドット絵フォントに戻せる可能性がある。
- 画像は**表示サイズちょうどの解像度で書き出す**か、`react-native-svg` で矩形として描く（RNの `Image` は拡大時に平滑化されるため）。
  - **実際に後者を採用した。** `src/components/pixel/icon.tsx` が、1文字=1ドットの
    文字列パターン（`'..XXXX..'` など8文字×8行）を `<Rect>` に展開する。
    画像ファイルを持たないので、どのサイズでも1ドットが正方形になりにじまない。
    **パターンには `.` `X` `o` 以外を混ぜないこと**（半角スペースが主色で塗られる事故が起きた）。
- **タブバーのラベルは `TAB_LABEL`（ゴシック10px）のまま。** アイコンが付いた後に
  17pxのドット絵フォントへ戻せるか再検討したが、「プロジェクト」を短縮すると
  要件定義書の用語（プロジェクト＝動画1本）とずれるため見送った。
- カレンダーは `react-native-calendars` を使わない。**自前でFlatList＋7列グリッド**を組む（見た目の作り込みに制約が出るため、結果的にこの方が速い）。
- **背景の風景は画面の地に敷いてよい**（2026-08-31 に変更）。
  以前は「ヘッダー帯と空状態だけ」に限定していた。要件定義書 12.6 #6 が
  「縦スクロールする画面に敷くと破綻する」と警告していたためだが、
  `components/app-background.tsx` は**画面に固定でスクロールしない**ので破綻しない。
  コンテンツは不透明な羊皮紙のパネル（`ui/panel.tsx`）で覆うので可読性も落ちない。
  **リスト領域そのものに風景を敷いてはいけない**という約束は維持する。

#### 1画面に収める（2026-08-30。実機で見切れを踏んで対応）

- **カレンダーのヘッダーは2行まで。** 「月＋月送り」「今日／新規登録／ベル／設定」。
  以前は月ラベル・週表示トグル・ボタン・メンバー行で4行あり、
  **iPhone Safari で6行のグリッドが最初の画面から押し出されていた**。
  縦の余裕が無い画面では、ヘッダーに行を足す前に何を削るかを先に考えること。
- **週表示トグルは広い画面だけ。** 中身が未実装のスタブに、狭い画面で1行使わない。
- **メンバー行はカレンダーに置かない。** PCはサイドバー、モバイルはメンバー画面にある。
- **「選択日の予定」は下部パネルの1枚目に統合した。** 以前はカレンダー直下に別セクションが
  あり、初期選択が今日なので「今日の予定」パネルと同じ内容が2回出ていた。
- **広い画面はスクロールさせない。** `Body` が幅で `ScrollView` と `View` を切り替え、
  グリッド領域が `flex: 1` で残りの高さを取り、**セルの高さ＝その高さ÷6行**で決まる。
  6行固定なので月が変わっても高さは動かない（上の約束は保たれる）。
  パネル行は `LAYOUT.panelRowHeight` の固定高さで、溢れる中身は各パネル内でスクロールする。

#### レスポンシブ（2026-08-30）

- **900px（`LAYOUT.sidebarMinWidth`）以上で左サイドバー、未満で下タブ**に切り替える。
  同梱の bottom-tabs が持つ `tabBarPosition: 'left' | 'bottom'` に乗せ、
  **自前で `flexDirection: 'row'` に包み替えない**（ライブラリにレイアウトを任せる）。
- 900px の根拠: サイドバー256pxを引いた残りで、カレンダー7列が
  チップ表示（`compactCellWidth` 72px）を保てる幅。
- **カレンダーのセル幅は `useWindowDimensions` ではなく `onLayout` の実測を使う。**
  ウィンドウ幅で計算すると、サイドバーが出ているときにその幅ぶん余分に見積もり、
  7列目（土曜）がはみ出して切れる（実際に踏んだ）。

#### カレンダーの確定事項（S0）

- **7列 × 6行 = 42セル固定。** 月によって5行/6行が変わると、月送りのたびに高さが変わり
  下のコンテンツ（予定リスト・将来の負荷サマリー）が飛ぶ。
- 週の開始は**日曜**。
- 隣接月（前月末・翌月頭）は**日付をグレーで出すだけで、予定は表示しない。**
  隣接月の予定が見たければ前月/次月ボタンで移動する。タップ時の挙動の期待も揺らがない。
- **曜日は濃茶の帯に白文字。その帯の中でだけ日=赤・土=青にする**（2026-08-31 に変更）。
  以前は「曜日の色分けは行わない」としていた（日曜赤・土曜青が予定種別の
  赤=ロング公開・青=撮影と衝突するため）。予定種別はチップの**色枠と淡色地**が
  示すようになり、濃茶の帯の上の文字色とは見え方がはっきり違うので混同しない。
  色は `COLORS.weekdaySun` / `COLORS.weekdaySat`（帯が暗いので明度を上げてある）。
  **セルの背景には引き続き色を敷かない。**
  （旧ルール: ヘッダーの「日」「土」の文字色だけ `COLORS.textWeekend`（茶）に変える）
  一般的な日曜赤・土曜青は予定種別の赤（ロング公開）・青（撮影）と意味が衝突するため。
  セル背景に色は敷かない。必要になればP2以降で再検討する。
- 今日は**色ではなく硬い矩形の枠**で示す。色で示すと予定種別の色と衝突するため。
- セル幅が `LAYOUT.compactCellWidth` 未満なら文字を捨ててドット表示に切り替える（要件定義書 12.3）。
- 出欠の集約の優先順位は **欠席あり > 未回答あり > 未確定あり > 全員出席**。対応が必要な順。
  未回答はリマインド再送の対象（要件定義書 F7）なので maybe より優先度を上げている。
- **グレーアウトした隣接月のセルもタップできる。** その月へ移動し、その日を選択する。
  「日付をタップすれば予定が見える」挙動をカレンダー全体で揃えるため。
  予定を出さないのは表示の作り込みを増やさないためであって、タップを殺す理由ではない。
- 月送りボタン（前月/次月）では**選択を解除する。** 移動先の月に選択日が無く、状態が噛み合わないため。
  「今日」ボタンは当月に戻したうえで今日を選択する。
- 選択セルは**背景色**（`COLORS.surfaceSunken`）、今日は**硬い矩形の枠**で示す。
  役割が違うので同時に成立し、どちらも予定種別の色と衝突しない。

#### 出欠の記号は2セットある（混同しないこと）

| | 対象 | トークン | 記号 |
|---|---|---|---|
| 集約 | 配信1件を4人ぶんまとめた結果 | `ATTENDANCE_STATUS` | ○ / ⚠ / ✕ / ― |
| 個人 | メンバー1人の回答 | `ANSWER_BADGE` | ○ / **△** / ✕ / ― |

**maybe だけ記号が違う。** 集約側の `⚠` は「対応が必要」という警告の意味を持たせているため、
個人の回答に流用すると「この人の回答自体が問題」という誤読を招く。`△` なら「未定」を表すだけで済む。
欠席（✕）と未回答（―）は「個人の状態＝集約の材料」の関係がそのまま成り立つので同じ記号でよい。

### 3.2 メンバー名をコードに直接書かない

- 「けん」「らてん」「南場テル」「ゆず」を**ソースコードに文字列リテラルとして書かない。**
  表示名・役割・色・アイコンはすべて `Member` 型のデータから読む。
- P0では `src/data/dummy.ts` から読んでいた。P1で `src/lib/api.ts` 経由 `members` テーブルに差し替え済み。
  **この差し替えでコンポーネントが変わらない構造にした。**
- 標準担当者も同様。工程テンプレートは**テンプレートIDと工程の配列**という形で持ち、担当者はロールまたはIDで参照する。
  - **P5で実装済み**（`src/lib/task-template.ts`）。要件定義書 第5章の表は標準担当を実名で
    書いているが、コードには `defaultRole: '企画'` のように **`Member.role` の文字列**で持ち、
    一致するメンバーが居なければ `assignee_id: null`（未割当）にしている。
    工程名（「建築・コマンド準備」等）は**テンプレートのデータ**として持ち、
    `TaskKind` の enum は汎用のまま（`build` など）にしてある。
- メモクラ固有の用語（建築・コマンド等）を工程名に固定しない。テンプレート側のデータとして扱う。
- 理由: P8のマルチチーム対応。後から剥がすと全画面の書き換えになる。

### 3.3 Minecraftのテクスチャ・意匠を使わない

- **Mojang / Microsoft のテクスチャ画像・音声をアプリに組み込んで配布してはいけない。** 公式ガイドラインで認められていない。
- 特に避ける: 草ブロック / 土 / 木材のテクスチャ、ダイヤのツルハシ、チェスト、エメラルド、**クリーパーの顔**。
- 配信予定のアイコンは、マイク / テレビ / 電波マーク / 魔法陣 / ランタン など**自作のRPG風モチーフ**にする。
- RPG風ドット絵（木枠・羊皮紙・ドット絵キャラ）はすべて自作可能なので、この方針で問題は解消する。
- ロゴ（メモリークラフト）は自作なので使用可。起動画面やお知らせの空状態に使う。
- 理由: 将来の配布・課金（P8）を見据えると、後から全アセットを差し替えるのは相当な手戻りになる。

### 3.4 色だけで意味を伝えない。必ず記号やアイコンを併用する

一度決めた色の意味は全画面で変えない。

| 用途 | 色 | 併用する記号 |
|---|---|---|
| ロング動画公開 | 赤 | 種別アイコン |
| ショート動画公開 | 紫 | 種別アイコン |
| 配信予定 | 緑 | 種別アイコン |
| 撮影予定 | 青 | 種別アイコン |
| 出欠：全員出席 | 緑 | ○ |
| 出欠：未確定あり | 黄 | ⚠ |
| 出欠：欠席あり | 赤 | ✕ |
| 出欠：未回答あり | 灰 | ― |
| **主アクション・選択中** | **青（`COLORS.primary`）** | **ボタンの立体＋文字の反転** |

- **`COLORS.primary`（青）は「その画面で一番やってほしい操作」と「いま選ばれているもの」にだけ使う**（2026-08-31 追加）。
  おまけの操作にまで使うと、どれが主なのか分からなくなる。**1画面に1〜2個まで。**
  予定種別の「撮影（青）」と同じ色だが、**予定チップの中では使わない**（チップは色帯とアイコンで種別を示す）ので衝突しない。
- **選択中を色だけで示さない。** チップは地の沈み＋枠の太さ、下タブは地の沈み＋上辺の太線、
  サイドバーは地の明るさ＋左の縦線、というように**必ず形でも示す**。
- **予定種別の色とメンバー識別色で青と緑が重複している。** メンバー識別は必ずドット絵アイコンとセットで表示し、色だけで判別させない。
- **負荷グラフのバーはメンバーの識別色のまま変えない。** 集中していても色を警告色に振り替えない
  （「色＝その人」の対応が崩れて誰のバーか読めなくなる）。警告は ⚠ と「全体の57%」の言葉で出す。
- **出欠ステータスは4種類。凡例を画面ごとに変えないこと。** （モックアップでは上部と下部の凡例がずれている。実装では4種類で統一する）
- メンバー識別色はコードに書かず `Member.color` から読む（3.2と同じ理由）。

### 3.5 APIキーをリポジトリに含めない

**P1に入ったので「Supabaseクライアントを入れない」制約は解除された。**
ただし鍵の扱いは恒久ルールとして残る。

- **接続情報は `.env` から読む。ソースにハードコードしない**（要件定義書 第8章）。
- `.env` は `.gitignore` で除外済み。コミットしてよいのはキー名だけの `.env.example`。
- Expoの `EXPO_PUBLIC_` 付き環境変数は**バンドルに埋め込まれ、利用者から見える**。
  ここに置いてよいのは anon キーだけ。**`service_role` キーは絶対に置かない**（RLSを無視できるため）。
- anonキーは公開前提。**データを守るのはRLSであって、キーの秘匿ではない。**
  RLS は `0003_rls.sql` で有効化する（6テーブル・`team_id` 単位で分離。
  判定は `current_team_id()`（security definer）。書き込みは availabilities/notifications のみ
  本人スコープ、他は同一チームで自由。役割ベースの制限は P5 以降）。
  Vercel の環境変数にも anon キーだけを入れる（`memocra-schedule` プロジェクト）。
- **Gemini の API キーは Supabase のシークレットに置く**（`supabase secrets set GEMINI_API_KEY=...`）。
  `EXPO_PUBLIC_` には**絶対に置かない**（バンドルに焼き込まれて誰でも使えてしまう）。
  Edge Function も **`service_role` キーは使わない。** 呼び出し元のユーザーの JWT で
  Supabase クライアントを作れば RLS がそのまま効く（`supabase/functions/_shared/auth.ts`）。

（P0での制約は「Supabaseクライアントを入れない／ネットワークリクエストを書かない／
状態はローカルのみ」だった。UIの検証を先に終わらせるためで、目的は果たした）

---

## 4. ディレクトリ構成の方針

Expo SDK 57 のデフォルトテンプレートに合わせ、アプリコードは `src/` 配下に置く。
`tsconfig.json` の `@/*` エイリアスが `./src/*` を指すため、インポートは `@/types` のように書く。

```
.
├── CLAUDE.md                  # このファイル。開発ルール
├── メモクラ_..._要件定義書_v0.4.md  # 仕様の出典。編集しない
├── app.json / package.json / tsconfig.json
├── vercel.json                # web(SPA)を Vercel にデプロイする設定。`expo export -p web` → dist/
├── .env                       # 接続情報。**gitignore済み。コミット禁止**
├── .env.example               # キー名だけのテンプレ。これはコミットする
├── assets/                    # 画像。ドット絵は表示サイズちょうどで書き出す
├── docs/                      # GitHub Pagesで公開する静的ページ（OAuth同意画面の
│   ├── index.html             # ホームページ／プライバシーポリシーURL用。アプリ本体ではない）
│   ├── privacy-policy.html    # 連絡先 umi547303@gmail.com（差し替え済み）
│   └── .nojekyll
├── supabase/
│   └── migrations/
│       ├── 0001_init.sql      # 手でSQL Editorに貼って実行する。CLIは使っていない
│       ├── 0002_auth.sql      # 合言葉・認証トリガー。同じくSQL Editorで手動実行
│       └── 0003_rls.sql       # RLS有効化 + team_id単位の正式ポリシー。冪等。手動実行
└── src/
    ├── app/                   # Expo Router。ルーティングと画面の組み立てのみ
    │   ├── _layout.tsx        # フォント読み込みとログイン状態でのStack出し分け
    │   ├── index.tsx          # /calendar へのリダイレクトのみ（ログイン後にのみ見える）
    │   ├── login.tsx          # ログイン画面（Googleのみ）。web/nativeでOAuth往復方式を分岐
    │   ├── login-callback.tsx # web で Google から戻る先。detectSessionInUrl の繋ぎ表示（§5.3）
    │   ├── claim.tsx          # 合言葉入力画面。ログイン済みだがmembers行が無い人だけ見える
    │   ├── stream/
    │   │   └── [id].tsx       # S7 配信詳細・出欠。自分の回答/編集/削除。_layout に明示登録
    │   ├── project/
    │   │   └── [id].tsx       # S3 プロジェクト詳細。工程タスク一覧。_layout に明示登録

    │   └── (tabs)/            # PCはサイドバー13項目／モバイルは下タブ5項目
    │       ├── _layout.tsx    # 幅で tabBarPosition と tabBar を出し分ける
    │       ├── calendar.tsx   # S0 月カレンダー＋下部パネル
    │       ├── home.tsx       # S1 ホーム（今週）。未回答の出欠を最上部に
    │       ├── projects.tsx   # S2 プロジェクト一覧
    │       ├── tasks.tsx      # タスク一覧（全プロジェクト横断・絞り込み）
    │       ├── new.tsx        # 企画／配信予定の登録＋「文章から作る」（AI）
    │       ├── availability.tsx # S7 配信・出欠
    │       ├── workload.tsx   # S5 負荷ダッシュボード
    │       ├── members.tsx    # S6 メンバー。自分の行だけ編集可（RLS由来）
    │       ├── notifications.tsx # S8 お知らせ（中身はP4）
    │       ├── goals.tsx      # 目標（チーム/個人/企画 × 短期/中長期）
    │       ├── ai.tsx         # AI（壁打ち・進捗まとめ）
    │       ├── meetings.tsx   # 議事録（録音ファイル or 文字起こし → 要約）
    │       └── settings.tsx   # 設定・ログアウト・外部カレンダー・各画面への導線
    ├── components/            # 表示専用のUI部品。データ取得を書かない
    │   ├── app-text.tsx       # 既定フォント付きText。RNのTextを直接使わない
    │   ├── async-state.tsx    # 読み込み中/失敗時の共通表示（LoadingView / ErrorView）
    │   ├── coming-soon.tsx    # 未実装タブの「準備中」表示
    │   ├── event-card.tsx     # 選択日の予定カード。onPress を渡すとタップ可になる
    │   ├── stream-form.tsx    # 配信の作成・編集フォーム（両方で共用）
    │   ├── calendar-legend.tsx # 凡例（種別4＋出欠4）。チップ表示の幅のときだけ出す
    │   ├── schedule-chip.tsx  # カレンダーのセルに入る予定チップ
    │   ├── schedule-summary.tsx # 今日の予定／今週の公開予定／締切タスク（自分）
    │   ├── project-form.tsx   # 企画の作成・編集フォーム（両方で共用）
    │   ├── task-row.tsx       # 工程1行。担当とステータスを変更。ブロック中は理由必須
    │   ├── workload-summary.tsx # 負荷の横棒グラフ。硬い矩形で描く（§3.1）
    │   ├── goal-card.tsx      # 目標1件。進捗バー＋「短期/中長期」の言葉と数字（§3.4）
    │   ├── ai-compose.tsx     # 文章から登録フォームを埋める。**登録はしない**
    │   ├── replan-panel.tsx   # 締切・担当の調整案。**差分を見せてから適用**
    │   ├── external-calendars.tsx # ICS URLの登録と取り込み（設定画面用）
    │   ├── nav/               # ナビゲーション。幅で形が変わる
    │   │   ├── items.tsx      # 13項目の定義。下タブに出す5つの指定と、
    │   │   │                  # 出ない画面への導線の約束もここに書く
    │   │   ├── sidebar.tsx    # PC用。ロゴ・メンバー・13項目・自分のカード（Lv）
    │   │   └── bottom-bar.tsx # モバイル用。5項目だけ
    │   └── pixel/             # ドット絵の基本部品（枠・ボタン・アイコン等）
    │       ├── frame.tsx      # 木枠。四隅の金具＋辺のborderWidth
    │       ├── icon.tsx       # PixelIcon / MemberAvatar / DotGrid。SVGの矩形で描く
    │       ├── scenery.tsx    # 背景の風景（空・草原・城）。置き場所は§3.1の3つだけ
    │       └── date-picker.tsx # DateField / TimeField。OSネイティブは使わない（角丸）
    ├── constants/             # theme.ts（色・余白・フォントサイズ）など不変の定数
    ├── lib/                   # 日付計算・締切逆算・ステータス判定などのロジック
    │   ├── api.ts             # Supabaseから読む取得関数。P1の移行先。すべてasync
    │   ├── auth.tsx           # SessionProvider + useSession()。ログイン状態とmembers登録状態を
    │   │                      # Context で共有（_layout と claim が同じ state を見る必要がある）
    │   ├── calendar.ts        # 42セルのグリッド計算。6行固定。週の範囲（weekRangeOf）
    │   ├── task-template.ts   # 工程テンプレートと締切逆算。標準担当は role 参照（§3.2）
    │   ├── project-status.ts  # タスクの進捗からプロジェクト状態を導く（F3）
    │   ├── workload.ts        # メンバー別の負荷集計。50%以上で警告（F4）
    │   ├── level.ts           # 加点型のレベル。完了タスク数だけで決まる。HPは作らない
    │   ├── goal.ts            # 目標の進捗・仕分け。level.ts とは混ぜない
    │   ├── date-input.ts      # 日付の検証とISO組み立て（2フォームで重複していた）
    │   ├── ai.ts              # Edge Function `ai` の呼び出し口。**提案を返すだけ**
    │   ├── schedule.ts        # 3ソース＋外部カレンダーのマージと出欠の集約
    │   └── supabase.ts        # Supabaseクライアント。接続情報は .env から読む
    └── types/                 # データモデル（index.ts）
```

### 各階層のルール

- **`src/app/`** — ルーティングと画面の組み立てだけ。ビジネスロジックを書かない。1ファイル1ルート。
- **`src/components/`** — propsを受け取って描くだけ。**データの取得元を知らないこと。** これがP0→P1の差し替えを可能にする。
  - テキストは `react-native` の `Text` ではなく **`@/components/app-text` の `Text` を使う。**
    RNにはフォントをアプリ全体へ一括適用する仕組みが無く、React 19で `defaultProps` も廃止されたため、
    既定のドット絵フォントはこのラッパーでしか効かない。
  - **`src/components/ui/` に共通部品がある（2026-08-31 新設）。画面ごとに同じ部品を
    作り直さないこと。** 刷新前はボタンが12定義・チップが6定義・カードが4系統あり、
    `buttonText` は13ファイル、`disabled` は8ファイルで同一のものが書かれていた。
    | 部品 | 使う場面 |
    |---|---|
    | `Screen` / `ScreenHeader` / `SectionHeading` | すべての画面の外枠。**背景は塗らない**（風景を透かす） |
    | `Panel` | 枠付きの面。カード・パネルはすべてこれ |
    | `Button` | 実行する操作。primary(青)/secondary/danger/ghost |
    | `Chip` / `ChipRow` | 選ぶ・絞り込む |
    | `StatusBadge` / `NotifyBadge` | 色地に記号／赤丸の件数 |
    | `ProgressBar` / `ListRow` / `EmptyState` / `Input` / `SegmentedControl` | それぞれ1種類だけ |
    | `Avatar` / `MemberChip` | メンバーの表示。画像が無ければドット絵にフォールバック |
  - `src/components/pixel/` は**ドット絵の描画そのもの**（`DotGrid` / `PixelIcon` /
    `PixelFrame` / 風景 / 日時ピッカー）。`ui/` はそれを組み立てた画面部品。
- **`src/constants/theme.ts`** — 色・余白・フォントサイズを1ファイルに集約する。
  - 余白は**4の倍数**（4 / 8 / 12 / 16 / 24 / 32）
  - ドット絵フォントのサイズは**17 / 34 / 51**の3つだけ（にじみ防止、3.1参照）。
    増やすときも17の整数倍から選ぶ
  - **ゴシックは4つだけ**（2026-08-31 に2つ追加）。**fontFamilyとセットで持つこと。**
    サイズだけ取り出してドット絵フォントに当てると、避けたいにじみがそのまま起きる
    | トークン | サイズ | 使い所 |
    |---|---|---|
    | `TEXT.small` | 12 | メタ情報・補助ラベル。**これより小さくしない** |
    | `TEXT.body` | 14 | カードの本文・説明 |
    | `LONG_TEXT` | 15/lh24 | メモ・議事録・AIの出力のような長い文章 |
    | `TAB_LABEL` | 10 | 下タブのラベルだけ。ドット絵17pxだと「プロジェクト」が
      タブ幅（375pt端末で使える幅は65px）に収まらないため |
    `TEXT` を足した理由: 密度の高い画面（カレンダー下部のパネル、メンバーカード、
    負荷の内訳）を17pxで組むと行数が増えて溢れるのに、逃げ道が `LONG_TEXT` しか無かった。
    **これ以上増やさないこと。** 増やすほど画面ごとの揺れが戻る
  - **見出し・年月・時刻・件数・ボタンのラベルはドット絵**（17/34/51）。本文はゴシック。
    この使い分けがモックアップの印象に一番近い
  - 色は3.4のカラーマップをそのまま定数にする
  - **予定種別と出欠ステータスは「色＋記号」のセット（`Badge`）で持つ。**
    生の色（`palette`）はexportしない。色だけを取り出せる場所を作らないことで3.4を構造的に守る。
    `Badge.tint`（淡色地）は**色を増やしたのではなく**、同じ意味の色を面で塗るための濃度違い
  - **スタイル値をコンポーネント内にベタ書きしない。** 必ずここから読む。
- **`src/lib/`** — 純粋関数を置く。UIに依存しない＝テストしやすい単位で書く。
  `api.ts` はSupabaseから読む取得関数（すべて非同期）。P0の `src/data/dummy.ts`（ダミーデータを
  同期で返す形）はP1で全関数をここへ移し終え、削除済み。
- **`src/types/`** — 要件定義書 第7章のデータモデル。DBスキーマの変更はここに反映する。

### 命名

- ファイル名は kebab-case（`month-calendar.tsx`）。テンプレートの既定に合わせる。
- 型は PascalCase、単数形（`Member`、`Task`）。
- 日時のフィールドは `_at` 接尾辞（`publish_at`）。DB列名とアプリ側の型を一致させ、変換層を作らない。

---

## 5. 各フェーズへの申し送り

### 5.1 tasks / availabilities の team_id はトリガーで自動セットする（P1・対応済み）

**`supabase/migrations/0001_init.sql` で実装済み。** 以下は判断の記録。

`Task` と `Availability` の型には `team_id` を持たせている（要件定義書 第7章の定義には無い）。

- **理由:** P1でRLSポリシーを書くときに、親テーブル（`projects` / `streams`）へのサブクエリが不要になるため。
- **リスク:** 親子で `team_id` がずれる事故が起きうる。`tasks.team_id` が親の `projects.team_id` と違う値になると、RLSをすり抜けるか、逆に本人のデータが見えなくなる。
- **対応方針（確定）:
  `tasks` / `availabilities` の `team_id` は、INSERT / UPDATE 時に親レコード
  （`tasks` → `projects`、`availabilities` → `streams`）から
  **トリガーで自動セットする。** クライアントから渡された値は信用せず、常に上書きする。**
- あわせて、親子の `team_id` 一致を保証する制約（親テーブルに `UNIQUE(id, team_id)` を張り、
  子から複合外部キーで参照する）も検討する。トリガーより強く、DBレベルで不整合を防げる。

### 5.2 データ取得関数の async 化（P1・対応済み）

**完了。** `src/data/dummy.ts` は削除済み、`src/lib/api.ts` に全関数が移った。以下は判断の記録。

`src/data/dummy.ts` の取得関数は**同期**（`getProjects(): Project[]`）で作ってあった。
P0を最短で組むための判断で、Supabaseに差し替える時点で作業が発生した。

- `getProjects()` / `getStreams()` / `getAvailabilities()` / `getMembers()` の順に、
  `Promise` を返す形へ1つずつ切り替えた（同時に全部は変えず、都度ブラウザで動作確認してから次へ進んだ）。
- 呼び出し側（カレンダー画面 S0）には `useState` + `useEffect` で loading / error を持たせた。
  複数の取得先が同時に走るため、**エラーはどれか1つでも失敗したら1枚のカードにまとめ、
  再試行ボタンは全部を再取得する。** 4人しか使わないアプリで、失敗の理由ごとにカードを
  分けるほどの必要はないという判断（共通部品は `src/components/async-state.tsx`）。
- 取得関数の置き場所は `src/data/` から `src/lib/api.ts` に移した。全関数の移行が終わった
  時点で `src/data/dummy.ts` を削除し、`src/data/` ディレクトリごと片付けた。

### 5.3 認証（P1・対応済み）

**完了。** 以下は判断の記録。

**要件定義書との差分。** 要件定義書 第9章は「メールOTP（マジックリンク）」を認証方式としていたが、
**ユーザーの明示的な指示により Googleアカウントでのログインのみに変更した**
（そのほうがユーザーにとって楽なため。2026-08-27 決定）。メールOTPの画面は作っていない。

**制限方式は「許可リスト（メールアドレス事前登録）」→「合言葉」に変更した
（2026-08-27）。** 当初はメールアドレスの許可リストで制限する設計にしたが、
4人ぶんのメールアドレスを事前に集めるのが難しかったため。**問題はGoogleログイン
そのものではなく「事前にメールアドレスを集めて登録する下準備」だった**ことを
ユーザーに確認したうえで変更した。`supabase/migrations/0002_auth.sql` はこの時点で
まだ一度も適用していなかったため、新しいマイグレーションを足さず、そのファイル自体を
書き換えている（0001_init.sqlをpgcrypto修正した時と同じやり方）。

- **Googleアカウントであれば誰でも `auth.users` 行（アカウント）は作れる。**
  ただしそれだけでは `members` 行は作られず、アプリの中にも入れない。
- ログイン後、**4人だけが知っている合言葉**を入力する画面（`src/app/claim.tsx`）を挟む。
  合言葉は `team_passcode` テーブル（1行だけ）に平文で持つ。
  **これは見知らぬ人を締め出すためのもので、本気の攻撃者を防ぐものではない**
  （本当の意味でデータを守るのはRLSの役目、CLAUDE.md §3.5と同じ考え方）。
- 合言葉が合っていると、`claim_membership()`（`security definer`）が `members` 行を
  自動生成する（`id` は `auth.users.id` と同じ、`name` はメールのローカル部分、
  `color` は暫定色）。**本名・役割・識別色は、あとでSupabaseのTable Editorから
  手直しする。** 4人しかいないので、この一手間はコストに見合う。
  同じ関数を2回呼んでも（二重送信・再入力）何もしないだけで安全（冪等）。
- `members.id` の外部キー（`references auth.users(id) on delete cascade`）は
  制限方式に関係なく維持している（データの整合性の話であってアクセス制限とは別）。
- 合言葉方式なら**メールアドレスを1件も事前登録しなくてよい**。
  LINE/Discord等で4人にまとめて伝えるだけで済み、個別に聞いて回る必要が無い。
- アプリ側は `src/lib/auth.tsx` の `SessionProvider` / `useSession()` が、ログイン状態と
  「`members` 行があるか」の両方を持つ。合言葉画面はこの2つ目の状態だけを見て
  出し分けられる。`_layout.tsx` / `claim.tsx` / `login-callback.tsx` が**同じ state** を
  参照する必要があるため Context にしている（当初はフックだったが呼び出しごとに別 state になり、
  合言葉を通しても `_layout` 側が再評価されずリロードするまでカレンダーに進めなかった。
  2026-08-29 に Context 化して解消）。

**アプリ側。** `src/lib/supabase.ts` で `persistSession: true` / `autoRefreshToken: true` にし、
ネイティブでは `AsyncStorage`、web は supabase-js 既定の localStorage にセッションを保存する
（`storage` を web で渡さない）。web 出力は `output: "single"`（SPA）で Node プリレンダリングを
経由しないため、かつて `static` で起きた `window is not defined` は発生しない。
ログインの出し分けは `expo-router` の `Stack.Protected`。**未宣言のルートは保護の対象外になる**
ため、ログイン後に見せたい `(tabs)` は明示的に `Stack.Screen` として書くこと
（`login-callback` は逆に未宣言のまま＝常時アクセス可にしている）。

**web / ネイティブでログインの往復方式が違う（2026-08-28）。**
- **web**: `signInWithOAuth` のフルページ遷移で Google へ飛び、`/login-callback` に戻る。
  `src/app/login-callback.tsx` が戻り URL の `?code=` を読んで `exchangeCodeForSession` を
  自前で呼ぶ（10秒で戻らなければ・失敗すればログイン画面へフォールバックし、失敗時は
  Supabase のエラー文言をそのまま表示）。
- **native**: `expo-web-browser` の `openAuthSessionAsync` で開き、戻り URL を受け取って
  `exchangeCodeForSession` を自前で呼ぶ。
- `supabase.ts` の `detectSessionInUrl` は web / native とも **false**（自動処理と手動交換が
  二重になり「code 使用済み」で失敗するのを防ぐ）。
- 両方 `flowType: 'pkce'`。code verifier は storage（web は localStorage）に入るので
  フルページ遷移でも復元できる。
- **web の戻り先 `<origin>/login-callback` は Supabase の Redirect URLs に登録が必要**
  （`http://localhost:8081/login-callback` と本番 Vercel ドメイン）。

**OAuth同意画面は本番公開する（2026-08-28 決定）。** テストユーザー方式に戻すと
4人ぶんのGoogleアドレス収集が必要になり、合言葉方式に変えた意味が無くなるため。
本番公開には「アプリのホームページURL」と「プライバシーポリシーURL」が必須で、
これを **GitHub Pages（リポジトリの `docs/` フォルダ）** で用意した
（`docs/index.html` / `docs/privacy-policy.html`）。公開URLは
`https://<ユーザー名>.github.io/memocra-schedule/`。
アプリ内の入場制限は引き続き合言葉（`claim.tsx`）に一本化。Googleは認証のみ担当。
`docs/privacy-policy.html` の連絡先は実アドレス（`umi547303@gmail.com`、メモクラ用の
新規Gmail）に差し替え済み。公開URLは
`https://kairi072900-netizen.github.io/memocra-schedule/`（GitHub Pages・`main`/`docs`）。

**私には確認できないこと。** Google Cloud ConsoleでのOAuthクライアント作成、Supabase Dashboard
でのGoogleプロバイダ設定、GitHubリポジトリ作成とPages有効化、同意画面のブランディング設定と
本番公開の切り替えは私にはできない。実際にGoogleでログインが最後まで成功するかどうかも、
本物のGoogleアカウントでのブラウザ操作が必要なため私は検証できない。型チェック・DBトリガーの
動作（PGliteで検証済み）・ログイン前の画面表示（未ログインならログイン画面のみ、`/calendar`や
`/claim`へ直接アクセスしても入れないことをブラウザで確認済み。`claim.tsx` の画面自体も
ガードを一時的に外して目視確認した）までで、実際のOAuth往復・合言葉の入力が最後まで
通るかはユーザー自身の実機・ブラウザでの確認が要る。§7.1（Expo GoがSDK 57未対応）も
ここに影響する。Googleログインはアプリに「戻ってくる」ディープリンクが必要なので、
Expo Goでの動作確認は従来のドット絵フォント確認と同じ理由でできない可能性が高い。

### 5.4 P1のその他

- 全テーブルに `team_id` を持たせる。RLSは `team_id` 単位で分離する。
- **テーブル名は `profiles` ではなく `members`。** 要件定義書 第7章は `profiles` だが、
  アプリ側の型名 `Member` と揃える実利を優先して変更した（2026-08-21 決定）。
- `members.id` は `auth.users.id` と同じUUID。**外部キーは認証実装時に張った**（§5.3）。
- 認証は**Googleアカウントのみ**（メールOTPから変更。§5.3）。
- カレンダー専用テーブルは作らない。`projects.publish_at` / `projects.shoot_at` / `streams.starts_at` の3つを日付でマージして表示する（予定の二重管理になるため）。
- 工程テンプレートはMVPではコード内の定数。ただし**テンプレートIDと工程の配列**という形にしておき、後からDB化しやすい構造にする。
- APIキーは環境変数に置き、リポジトリに含めない。

### 5.5 配信削除時に出欠回答が消えることの扱い（P2・対応済み）

**完了。** 以下は判断の記録。

`availabilities` は `streams` の削除で **`on delete cascade` により連鎖削除される。**
つまり**配信を1件消すと、メンバーが入力した回答も一緒に消える。**

- **対応（2026-08-29 決定）: 確認ダイアログを出す。** データは残さない（cascade のまま）。
  `src/app/stream/[id].tsx` の削除ボタンで
  「この配信には◯件の出欠回答があります。削除すると回答も消えます。」と件数を示して確認する
  （web は `window.confirm`、ネイティブは `Alert.alert`）。
- 論点だった「**他人の操作でメンバー本人の入力が消える**」点は、件数の提示で
  「何が失われるか」を操作者に見せることで担保する。取り消し（undo）や論理削除は作らない。
  4人しか使わないアプリで、間違えたら登録し直せば済むため。
- **権限は絞っていない（誰でも作成・編集・削除できる）。** 要件定義書 第3章は
  「削除はリーダーのみ」としているが、4人での運用では摩擦のほうが大きいと判断した。
  トラブルが出たら role ベースの制限を足す。

### 5.6 P5: 論理削除の検討

`tasks` は `projects` の削除で連鎖削除される。P1ではこれで進める（間違い登録の取り消しには適切）。
ただしP5でタスク管理を作るときに再検討すること。

- **問題:** 「終わった企画を整理する」用途で消すと、工程の履歴ごと失われる。
  成功指標（リーダーの担当タスク比率、公開予定日に対する遅延率）の測定に必要なデータが該当する。
- **代替案:** `deleted_at` 列による論理削除。履歴は残るが、全クエリに `deleted_at is null` が必要になる。
  P1の段階では明らかに過剰なので採用しなかった。
- P5で「過去の制作履歴をどこまで残すか」を決めてから、削除の設計を確定する。

---

## 6. 未決事項（該当箇所を実装する前に確認する）

要件定義書 第11章より。**勝手に決めずに聞くこと。**

1. 投稿頻度（週何本のロング／ショート）→ 同時進行プロジェクト数、一覧画面の設計に影響
2. メンバーの端末（iPhone / Android の内訳）→ Expo Goでの配布方法とテスト環境に影響（**P3の前に必要**）
3. 活動時間が限られるメンバーの締切を、活動可能日に丸めるかどうか → P5の締切逆算ロジックに影響
4. ~~配信は定期開催か不定期か。日程を先に決めてから出欠を取るのか、全員の空き日から決めるのか~~
   → **決定済み（2026-08-29）。「日時を先に決めて登録 → 出欠を取る」方式でP2を実装した**
     （要件定義書 F7 の既定どおり）。「候補日を複数出して調整する」（調整さん形式）は
     将来フェーズ。`stream_candidates` テーブルの追加が必要になるため、P2には入れていない
5. メンバーアイコンをRPG風ドットキャラにするか、本人のスキン頭部にするか
   → スキン頭部は本人のアイデンティティそのもの。**メンバー本人に確認すること**（ハイブリッドも成立する）

---

## 7. 既知の制約

### 7.1 Expo Go で実機確認ができない（2026-08-21 時点）

**本プロジェクトは Expo SDK 57（`expo@~57.0.15` / `react-native@0.86.2`）だが、
手元の Expo Go が SDK 57 に非対応で、実機で開けない。**

- 影響: **ドット絵フォント（DotGothic16）の実機での可読性が未検証。**
  特にカレンダーの日付（`FONT_SIZE.body` = 17px）と
  出欠バッジの記号（○ / ⚠ / ✕ / ―）が小さい画面で判別できるかを確認できていない。
- 現状の代替検証: Webで幅320pt・375ptでのレイアウトを確認済み。
  CSSピクセル幅は端末のポイント幅と一致するのでレイアウトは実機と同条件だが、
  **物理的な文字の読みやすさとフォントのラスタライズは端末でしか確認できない。**
- **対応: Expo Go がSDK 57に対応したタイミングで改めて実機確認する。**
  そこで読みにくければ `FONT_SIZE` とセルの高さを見直す（17の整数倍から選ぶこと、§3.1）。

**P3（配布）の方針（2026-08-28 決定）: web アプリ先行。**
要件定義書 第9章は「ネイティブアプリを Expo Go で配布」を前提にしていたが、
Expo Go の SDK 57 非対応に加え、Google ログインの戻りディープリンクも Expo Go では
通らない見込み。iOS 開発ビルドは Xcode（このMacに未インストール、Command Line Tools のみ）
または有料 Apple Developer 登録が要る。

→ **当面は Expo の web 出力（`output: "single"` の SPA）を Vercel にデプロイし、
4人は iPhone Safari / PC ブラウザで使う。** コードは Expo 共通のまま維持するので、
将来ネイティブ（iOS/Android 開発ビルド or ストア配布）に展開できる。
web 版で Google ログイン往復を確認するのが P1 完了・P3 着手の実質的な条件。

ドット絵フォントの実機ラスタライズ確認は、ネイティブ展開時に改めて行う（web では別問題）。

---

## 8. 作業の進め方

- 実装を始める前に、**作成・変更するファイルの一覧と役割を提示して確認を取る。**
- 1コミット1目的。壊れたときに戻れる粒度で刻む。
- ファイルを追加したら、このファイルの「P0の進捗」とディレクトリ構成を更新する。
- 型チェックは `npx tsc --noEmit` で通してからコミットする。
