# メモクラ スケジュール管理アプリ 要件定義書 v0.4

作成日: 2026-08-20 / 対象: グループYouTuber「メモリークラフト（メモクラ）」（4名）

### 更新履歴
- **v0.4** UIの方向性をRPG風ドット絵に確定。ゲーミフィケーション（Lv/HP）の扱いと装飾フレームの実装方針を追記
- v0.3 UIデザイン方針（第12章）を追加。モックアップを反映し、アセットの権利面の注意と技術リスクを追記
- v0.2 メンバーヒアリング結果を反映。カレンダー・配信出欠・通知をMVPに昇格し、開発順序を大幅に組み替え。将来の汎用化方針を確定
- v0.1 初版

---

## 1. 背景と課題

メモクラは4人のMinecraftクリエイターグループで、ロング動画・ショート動画・SNSを並行して運営している。
現状の運営で起きている問題は以下の3点。

| # | 課題 | 具体的に何が起きているか |
|---|---|---|
| A | リーダーへの業務集中 | 企画・ロング編集・SNS・最終判断がけんに集まり、チームの進行速度がけん個人の作業量に依存している |
| B | 撮影後の担当が決まらない | 撮影は全員でやるが、その後の「誰が・何を・いつまでに」が曖昧なまま解散し、結果的にけんが拾う |
| C | 進捗が見えない | 誰がどこまで進んでいるかが本人にしか分からず、金曜会議まで遅延が発覚しない |

**重要な認識:** これは「カレンダーがない問題」ではなく **「タスクの分解と割り当てが仕組み化されていない問題」**。
単なる予定表アプリを作っても課題A・Bは解決しない。本アプリは **動画1本ごとの制作パイプラインを管理するツール** として設計する。

### 1.2 メンバーヒアリング結果（v0.2で追加）

リーダー視点の課題A〜Cに加え、メンバー側から次の3点が挙がった。**これは課題A〜Cとは別種の、より切実なニーズ。**

| # | 声 | 読み取れる要求 |
|---|---|---|
| D | Discord・LINEだと他の通知に埋もれて気づかない。専用アプリからの連絡なら気づける | 重要な連絡の**到達率**を上げたい。チャット機能が欲しいわけではない |
| E | この日にどの動画が投稿されるか、ぱっと見て分かるようにしたい | チーム全体の**予定の見通し**が欲しい |
| F | この日の配信に誰が出られるか分かるようにしたい | **出欠の事前確認**を仕組みにしたい |

**この結果を受けてMVPの方針を変更する。**
課題A〜Cはリーダーの痛みであり、課題D〜Fはメンバーの痛み。**メンバーが毎日開きたくなる理由（=D〜F）を先に作らないと、タスク管理機能（=A〜C）は誰にも使われない。**
したがって開発順序を「カレンダー・出欠・通知 → タスク管理」に組み替える（第10章参照）。

---

## 2. 目的とゴール

### 目的
動画1本あたりの工程を自動で分解し、担当と締切を撮影当日に確定させることで、リーダーの抱え込みを防ぎ、制作を止めない。

### 成功指標（リリース3ヶ月後に測る）
- 担当者未定のまま24時間以上放置されるタスク数: **0件/週**
- けんの担当タスク比率: **60%以下**（現状は体感80%以上と想定）
- 公開予定日に対する遅延率: **20%以下**
- 金曜会議の所要時間: 現状比 **30%短縮**（進捗共有が事前に済むため）

---

## 3. 想定ユーザーと権限

| ユーザー | 主な使い方 | 権限 |
|---|---|---|
| けん（リーダー） | 企画登録、全体の負荷確認、最終判断 | 全操作可（プロジェクト削除・メンバー管理含む） |
| らてん | 自分のショート編集タスクの確認・完了報告 | 自分のタスク編集、全体の閲覧 |
| 南場テル | 建築・コマンド準備、投稿後の分析タスク | 同上 |
| ゆず | サムネ・素材撮影タスク。活動時間が限られるため締切設定に配慮 | 同上 |

権限は最小限に。「全員が全部見える／編集は担当者本人＋リーダー／削除はリーダーのみ」で十分。

---

## 4. スコープ

### MVP（最初にここまで作る）
1. **チームカレンダー**（投稿予定・配信予定・撮影日を月/週表示）
2. **配信の出欠確認**（○×△での回答、誰が出られるか一覧）
3. 動画プロジェクトの登録（タイトル・種別・公開予定日）
4. 工程テンプレートからのタスク自動生成 + 公開日からの締切逆算
5. タスクへの担当者割り当て・ステータス更新
6. **プッシュ通知**（出欠依頼・担当割り当て・締切リマインド）
7. 今週やることの一覧（自分ビュー／全体ビュー）
8. メンバー別の負荷ダッシュボード

### v2以降
9. 会議モード（金曜会議用の進捗サマリ＋議事録）
10. タスク単位のコメントスレッド
11. YouTube Data APIとの連携（公開後の再生数・CTRを自動取得して分析タスクに紐付け）
12. マルチチーム対応（他グループへの提供、チーム招待、テンプレート編集）
13. メモクラAI（チャット）との連携

### 今回やらないこと（明示的に除外）
- 動画ファイルそのものの保管・受け渡し（既存のクラウドストレージを使う）
- **自由記述のDM・雑談チャット**（理由は第6章 F5 を参照。LINE/Discordを置き換えない）
- 給与・収益分配の管理

---

## 5. コアコンセプト：動画1本 = 1プロジェクト

```
プロジェクト（動画1本）
  └ タスク（工程）×N  ← テンプレートから自動生成、担当者と締切を持つ
```

### 工程テンプレート例（ロング動画）

| 順 | 工程 | 標準担当 | 締切（公開日からの逆算） |
|---|---|---|---|
| 1 | 企画確定 | けん | -14日 |
| 2 | 建築・コマンド準備 | 南場テル | -10日 |
| 3 | 撮影 | 全員 | -7日 |
| 4 | 素材整理・共有 | 撮影担当 | -6日 |
| 5 | ロング編集 | けん | -3日 |
| 6 | サムネ作成 | ゆず | -3日 |
| 7 | タイトル・概要欄 | けん | -2日 |
| 8 | 投稿予約 | けん | -1日 |
| 9 | ショート切り出し | らてん | +2日 |
| 10 | SNS告知 | けん | 当日 |
| 11 | 投稿後分析 | 南場テル | +3日 |

### 工程テンプレート例（ショート単体）
企画 → 撮影/素材選定 → ショート編集（らてん） → 投稿 の4工程。

**この「テンプレート適用ボタン」が本アプリの中核機能。**
撮影が終わったその場でボタンを押せば、11個のタスクが担当者と締切つきで並ぶ。課題A・Bはこれで構造的に解決する。
※標準担当はあくまで初期値。画面上でいつでも変更できることが前提（役割の固定化を避けるため）。

---

## 6. 機能要件

### 6.1 画面一覧

| # | 画面 | 内容 |
|---|---|---|
| S0 | **カレンダー** | 月/週表示。投稿予定・配信予定・撮影日を種別で色分け。日タップで詳細 |
| S1 | ホーム（今週） | 自分の未完了タスク（締切順）＋今週公開予定の動画＋未回答の出欠依頼 |
| S2 | プロジェクト一覧 | 進行中の動画をステータス別に表示（かんばん or リスト） |
| S3 | プロジェクト詳細 | 工程タスク一覧、担当変更、締切変更、メモ |
| S4 | プロジェクト新規作成 | タイトル／種別／公開予定日／テンプレート選択 |
| S5 | 負荷ダッシュボード | メンバー別の未完了タスク数・今週の締切数を横棒グラフで表示 |
| S6 | メンバー | 4人の役割・活動時間帯の登録 |
| S7 | **配信詳細・出欠** | 配信の日時・内容と、メンバー4人の出欠（○×△）を一覧表示 |
| S8 | **お知らせ（通知一覧）** | アプリが発行した通知の履歴。未読バッジ付き |
| S9 | 会議モード（v2） | 今週完了／遅延中／未割当／来週の予定のサマリ＋議事録入力 |

### 6.2 主要機能の詳細

**F1. プロジェクト作成**
- 入力: タイトル、種別（ロング／ショート／SNS／その他）、公開予定日、撮影予定日（任意）
- 種別を選ぶと対応するテンプレートが自動選択される

**F2. タスク自動生成（逆算）**
- 公開予定日を基点に各工程の締切を自動計算
- 生成後、担当者は個別に変更可能
- 締切が過去日になる場合は警告を出す（例：公開まで3日しかないのに-14日の工程がある）

**F3. ステータス管理**
- タスク: `未着手` / `作業中` / `完了` / `ブロック中`
- `ブロック中` には理由を必須入力（例: 「素材が届いていない」）→ これが会議の議題になる
- プロジェクト: `企画中` → `撮影待ち` → `撮影済` → `編集中` → `投稿待ち` → `公開済` → `振り返り済`
- プロジェクトのステータスは、含まれるタスクの進捗から自動で判定する

**F4. 負荷の可視化**
- メンバー別に未完了タスク数と直近7日の締切数を表示
- 1人に全体の50%以上が集中している場合は色で警告（課題Aの再発検知）

**F5. 通知（MVPに昇格）**

メンバーの声Dへの回答。ただし **「専用アプリのDM機能」は作らない。** 理由は下記。

> LINE/Discordで埋もれるのは、そこに雑談・告知・業務連絡がすべて混在しているから。
> 同じものをアプリ内に作れば、**3つ目の埋もれる場所が増えるだけ**になる。
> 通知が「気づける」のは、量が少なく、全部が自分に関係あるからこそ。
> したがってこのアプリの通知は **自動発行される業務通知に限定し、自由記述のDMは実装しない。**

送る通知は次の5種類のみ（1人あたり多くても1日2〜3通に収まる設計）。

| 種類 | 発行タイミング | 宛先 |
|---|---|---|
| 出欠依頼 | 配信予定が登録されたとき | 全員 |
| 担当割り当て | 自分がタスクの担当者になったとき | 本人 |
| 締切リマインド | 締切前日の朝 | 担当者 |
| 締切超過 | 締切を過ぎた時点 | 担当者＋リーダー |
| 未割当の放置 | 担当者未定のまま24時間経過 | リーダー |

- すべての通知はS8「お知らせ」に履歴として蓄積され、未読バッジが付く
- タップすると該当のタスク／配信画面へ直接遷移する
- 「この件について相談したい」場合はv2でタスク単位のコメントスレッドを追加（文脈に紐づくので埋もれない）。雑談はLINE/Discordに残す

**F6. チームカレンダー（MVP）**

メンバーの声Eへの回答。開いた瞬間に「今月なにが出るか」が分かることを最優先にする。

- 月表示をデフォルトとし、週表示に切り替え可能
- 表示する予定は4種類。種別ごとに色を分ける
  - ロング動画の公開予定
  - ショート動画の公開予定
  - 配信予定（出欠の回答状況をアイコンで表示）
  - 撮影予定日
- 日付をタップすると、その日の予定と関連タスクが下部に展開される
- 自分に締切のあるタスクがある日には、識別できる印を付ける

**F7. 配信の出欠確認（MVP）**

メンバーの声Fへの回答。「誰が出られるか」を配信企画を立てる前に確定させる。

- リーダー（または誰でも）が配信予定を登録 → 全員に出欠依頼の通知が飛ぶ
- 各メンバーは `○出れる` / `△未定` / `×無理` の3択で回答。ひとことコメントを添えられる
- カレンダーと配信詳細画面に、4人の回答が顔アイコン＋色で並ぶ
- 未回答者がいる場合は、配信の前々日にリマインドを再送する
- ゆずのように活動時間が限られるメンバーがいるため、登録時に各メンバーの活動時間帯を参照できるようにする

---

## 7. データモデル（Supabase想定）

```
profiles
  id            uuid  (auth.users.id)
  team_id       uuid  ← 将来の汎用化に備えて最初から持たせる
  name          text
  role          text            -- 企画/ショート編集/建築/サムネ など
  color         text            -- UI上の識別色（ゆず=黄 / 南場テル=灰 / けん=青 / らてん=緑）
  avatar_url    text            -- スキン頭部画像。Supabase Storageに置く（コードに直接埋め込まない）
  active_hours  text            -- 例「平日20時以降」
  push_token    text

projects
  id            uuid
  team_id       uuid
  title         text
  kind          text            -- long | short | sns | other
  status        text
  shoot_at      timestamptz     -- 撮影予定
  publish_at    timestamptz     -- 公開予定
  owner_id      uuid            -- 企画責任者
  memo          text
  created_at    timestamptz

tasks
  id            uuid
  project_id    uuid
  kind          text            -- planning | build | shoot | edit_long | edit_short | thumbnail | upload | sns | analytics
  title         text
  assignee_id   uuid  nullable  -- null = 未割当（要通知）
  due_at        timestamptz
  status        text            -- todo | doing | done | blocked
  blocked_reason text
  sort_order    int
  done_at       timestamptz

streams                         -- 配信予定
  id            uuid
  team_id       uuid
  title         text
  starts_at     timestamptz
  duration_min  int
  platform      text            -- youtube | twitch | other
  memo          text
  created_by    uuid

availabilities                  -- 配信への出欠
  id            uuid
  stream_id     uuid
  member_id     uuid
  answer        text            -- yes | maybe | no | (未回答はレコードなし)
  comment       text
  answered_at   timestamptz
  UNIQUE(stream_id, member_id)

notifications                   -- お知らせ履歴
  id            uuid
  team_id       uuid
  recipient_id  uuid
  kind          text            -- availability_request | assigned | due_soon | overdue | unassigned
  title         text
  body          text
  link_type     text            -- task | stream | project
  link_id       uuid
  read_at       timestamptz
  created_at    timestamptz

meetings                        -- v2
  id, team_id, held_on, agenda, minutes
```

**カレンダー表示について:** `projects.publish_at` / `projects.shoot_at` / `streams.starts_at` の3つを日付でマージして表示する。カレンダー専用のテーブルは作らない（予定の二重管理になるため）。

- 工程テンプレートはMVPではコード内の定数として持つ（DB化はv2で検討）
- `team_id` を最初から入れておくことで、将来「他のグループYouTuberにも配布する」方向に進める

---

## 8. 非機能要件

| 項目 | 要件 |
|---|---|
| 同時利用 | 4名（Supabase無料枠で十分） |
| デバイス | スマホ優先。撮影直後にその場で割り振るため、モバイルでの操作性が最重要 |
| リアルタイム性 | 他メンバーの更新が数秒以内に反映されること（Supabase Realtime） |
| オフライン | MVPでは非対応。閲覧のみのキャッシュはv2以降 |
| セキュリティ | RLSで team_id 単位に分離。APIキーは環境変数、リポジトリに含めない |
| 可用性 | 業務システムではないため、多少の障害は許容 |

---

## 9. 技術スタック

picm!tでの実装経験をそのまま活かせる構成にする。学習コストを新機能の習得だけに集中させるため。

| 領域 | 採用 | 理由 |
|---|---|---|
| フロント | Expo (React Native) + Expo Router | picm!tと同構成。実機配布もExpo Goで可能 |
| バックエンド | Supabase (Postgres / Auth / RLS) | 同上。RLS設計の経験がそのまま使える |
| リアルタイム | Supabase Realtime | **今回の新規学習ポイント。** picm!tでは未使用 |
| 通知 | Expo Notifications + Supabase Edge Functions | v2で着手。定時バッチが必要 |
| 認証 | メールOTP（マジックリンク） | 4人固定なので匿名認証は不要。picm!tで実装済みの仕組みを流用 |

---

## 10. 開発フェーズ

v0.2で順序を組み替えた。**メンバーが欲しがっている機能（カレンダー・出欠・通知）を先に出し、早い段階で4人に配る。**
タスク管理から作ると、完成するまで誰も使えず、フィードバックが得られないまま作り込むことになる。

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| P0 | 画面設計・ダミーデータでカレンダーUI（ローカル状態） | S0が月表示で動き、予定が色分けされて見える |
| P1 | Supabase接続 + 認証 + RLS（profiles / projects / streams） | 4人がログインでき、予定が永続化される |
| P2 | 配信の出欠機能（availabilities） | 配信を登録→全員が回答→カレンダーで一覧できる |
| P3 | **実機配布・メモクラで試用開始（第1回）** | **4人が実際にカレンダーと出欠を1週間使う** |
| P4 | プッシュ通知（出欠依頼・リマインド） | 通知が届き、タップで該当画面に飛ぶ |
| P5 | タスク管理 + テンプレート自動生成 + 締切逆算 | 撮影終了→ボタン1つで11タスク生成 |
| P6 | 負荷ダッシュボード | 偏りが色で分かる |
| P7 | 会議モード・コメントスレッド | 運用して出た不満から優先度を決める |
| P8 | マルチチーム対応（汎用化） | メモクラ以外の1グループが自力で使い始められる |

**P3を最優先で迎える。** カレンダーと出欠だけでも、メンバーにとっては今より確実に便利になる。そこで実際に使われる状態を作ってから、タスク管理を足していく。

### 汎用化（P8）に向けて今からやっておくこと
- 全テーブルに `team_id` を持たせる（後から追加するとデータ移行が発生する）
- 「けん」「らてん」等の名前をコードに直接書かない。すべて `profiles` から読む
- 工程テンプレートはMVPでは定数で持つが、**テンプレートIDと工程の配列という形にしておき**、後からDB化しやすい構造にする
- メモクラ固有の用語（建築・コマンド等）を工程名に固定しない。テンプレート側のデータとして扱う

---

## 11. 未決事項（着手前に決めること）

~~1. **公開範囲**~~ → **決定済み。** まずメモクラ専用、将来的に他グループにも使える汎用アプリへ。マルチチーム対応はP8だが、`team_id` の設計だけ最初から入れる
~~2. **現在の運用ツール**~~ → **決定済み。** LINE/Discordは雑談・会話の場として残す。本アプリは業務通知・予定・タスクに特化し、置き換えを狙わない

3. **投稿頻度**: 週何本のロング／ショートを出しているか
   → 同時進行するプロジェクト数が決まり、一覧画面の設計に影響する
4. **メンバーの端末**: iPhone / Android の内訳
   → Expo Goでの配布方法とテスト環境に影響する
5. **ゆずの活動時間**: 締切の自動計算時に考慮するか（例: 締切を活動可能日に丸める）
6. **配信の頻度と決め方**: 配信は定期開催か不定期か。日程は先に決めてから出欠を取るのか、全員の空き日から決めるのか
   → 後者なら「候補日を複数出して調整する」機能（調整さん形式）が必要になり、設計が変わる

---

## 12. UI/UXデザイン方針

### 12.1 コンセプト

**RPG風のドット絵UI。** 木枠のフレーム、羊皮紙色の背景、ドット絵のキャラクター。
Mojangのテクスチャを一切使わずに「ドット絵の世界観」を成立させられるため、将来の配布・課金（P8）を見据えても安全な選択。
Minecraft自体がドット絵の世界なので、チームの世界観からも離れていない。

原則は3つ。

1. **角丸を使わない** — ピクセルの直角を活かす。影も1〜2pxの硬い矩形で表現する
2. **1マス=1情報** — カレンダーのセル、ステータス記号、メンバーアイコンをすべて正方形グリッドに揃える
3. **色で意味を固定する** — 一度決めた色の意味を全画面で変えない（下表）。ただし**色だけに意味を持たせない**（記号を併用する）

### 12.2 カラーマップ

| 用途 | 色 | 使う場所 |
|---|---|---|
| ロング動画公開 | 赤 | カレンダー、予定カード |
| ショート動画公開 | 紫 | 同上 |
| 配信予定 | 緑 | 同上 |
| 撮影予定 | 青 | 同上 |
| 出欠：全員出席 | 緑＋○ | 配信カード |
| 出欠：未確定あり | 黄＋⚠ | 同上 |
| 出欠：欠席あり | 赤＋✕ | 同上 |
| 出欠：未回答あり | 灰＋― | 同上 |
| メンバー識別 | ゆず=黄 / 南場テル=灰 / けん=青 / らてん=緑 | 負荷グラフ、アイコン枠 |

**注意1:** 予定種別の色（赤/紫/緑/青）とメンバー識別色（黄/灰/青/緑）で青と緑が重複している。
メンバー識別は必ず**ドット絵アイコンとセット**で表示し、色だけで判別させないこと。

**注意2:** 出欠ステータスは4種類。**凡例を画面ごとに変えないこと。**
（モックアップでは上部と下部の凡例で項目数と記号の対応がずれている。実装時に4種類で統一する）

### 12.3 レイアウト

**デスクトップ / タブレット**
- 左サイドバー固定（カレンダー / ホーム / プロジェクト / タスク一覧 / 配信・出欠 / 負荷ダッシュボード / メンバー / お知らせ / 設定）
- サイドバー下部に自分のアイコンとオンライン状態
- メイン領域は月カレンダー、その下に3カラム（今日の予定 / 今週の公開予定 / 負荷サマリー）
- ヘッダーにメンバー4人のアイコン、ビュー切替、新規登録ボタン、通知ベル（未読バッジ）

**モバイル（優先）**
- 上部にハンバーガー＋メンバーアイコン＋通知ベル
- コンパクトな月カレンダー（予定は**ドットのみ**で表現し、詳細は下に出す）
- 選択日の予定カード → 締切タスク（自分）→ 下部タブバー（ホーム / プロジェクト / ＋ / 出欠 / お知らせ）
- 中央の「＋」を新規登録のFABにする（picm!tの撮影FABと同じ構造）

**重要:** モバイルでカレンダーのセルに文字を入れようとしないこと。ドット＋選択日の詳細リスト、という分担を守る。

### 12.4 フォント

日本語のドット絵フォントが必要。候補は次の2つ。

| フォント | ライセンス | 備考 |
|---|---|---|
| DotGothic16 | SIL OFL / Google Fonts | Expoの`@expo-google-fonts`で導入可能。**第一候補** |
| PixelMplus | M+ ライセンス | 12px/10pxの2サイズ。より角ばった印象 |

- ドット絵フォントは**指定サイズの整数倍でのみ使う**（12px, 24px…）。中途半端な倍率でにじむ
- 本文が長くなる箇所（メモ、議事録、ブロック理由）は可読性を優先し、通常のゴシックに切り替えてよい

### 12.5 アセットの権利について（要注意）

**Mojang/Microsoftのテクスチャ画像を、アプリに組み込んで配布してはいけない。**

- Minecraftの公式ガイドラインでは、ゲーム内のテクスチャ・音声を自作ソフトウェアに転用することが認められていない
- 現時点ではメモクラ内部で使うだけだが、**将来他グループに配布・課金する方針（P8）がある以上、ここは最初から避けておく**べき。後から全アセットを差し替えるのは相当な手戻りになる
- 特に、草ブロック・土・木材のテクスチャ、ダイヤのツルハシ、チェスト、エメラルド、クリーパーの顔は、Mojangの意匠そのもの

**対応方針（v0.4で確定）:**
- **RPG風ドット絵に全面移行することで、この問題はほぼ解消した。** 木枠・羊皮紙・ドット絵キャラはすべて自作可能
- ただし**クリーパーの顔アイコンが「配信予定」に残っている**（今日の予定カード、モバイルの予定カード）。ここは差し替えが必要
  - 代替案：配信のモチーフはマイク、テレビ、電波マークなど。RPG風なら「魔法陣」「ランタン」なども合う
- アイコン全般を自分たちのモチーフに置き換える（例：チェスト→本／宝箱風の自作ドット絵、ツルハシ→自作）
- **メンバーのアイコンをRPG風ドットキャラにするか、本人のスキン頭部にするかはメンバーに確認すること。** スキン頭部は本人のアイデンティティそのもので、そちらに愛着がある可能性がある。フレームはRPG風・アイコンはスキン頭部、というハイブリッドも成立する
- ロゴ（メモリークラフト）は自作なので問題なし。起動画面やお知らせの空状態に使う

### 12.6 技術的な注意点（React Native）

| # | 論点 | 対応 |
|---|---|---|
| 1 | ドット絵の拡大でにじむ | RNの`Image`は拡大時に平滑化される。**表示サイズちょうどの解像度で書き出す**か、`react-native-svg`で矩形として描く |
| 2 | ブロック風の背景 | `ImageBackground`の`resizeMode="repeat"`で16pxタイルを敷き詰める |
| 3 | 枠線の表現 | 角丸なしの`borderWidth`＋濃淡2色の疑似的な立体枠で表現する |
| 4 | カレンダーライブラリ | 既製の`react-native-calendars`は見た目の作り込みに制約が出る。**自前でFlatList＋7列グリッドを組む**方が結果的に速い |
| 5 | **装飾フレーム（木枠）** | 本格的には9-slice（ninepatch）が必要だがRNは標準対応が弱い。**四隅の金具だけを`position:absolute`で小さな画像として置き、辺は単色の`borderWidth`で済ませる**。これで見た目の9割は再現でき、実装コストは1/5になる |
| 6 | **背景の風景（草原・城）** | 縦スクロールする画面に敷くと破綻しやすい。**ヘッダー帯と空状態の画面だけに限定**し、リスト領域は無地の羊皮紙色にする |
| 7 | モックアップの再現度 | モックアップは完成形のイメージ。**P0では月カレンダーと予定ドットだけを作る**。負荷サマリーや3カラムは後から足す |

### 12.7 ゲーミフィケーション（Lv・HP）の扱い

モックアップのサイドバーに `Lv.25` と `HP 88/100` がある。**採用する場合は、先に意味を定義すること。**

**意味を持たせない（単なる飾り）場合**
- 実害はないが、ユーザーは必ず「これは何の数字か」を考える。意味のないUIは認知の無駄になる
- 飾りとして残すなら、Lvだけにして「アプリの利用日数」など説明可能なものに紐づける

**意味を持たせる場合の注意 —— HPは負荷の指標にしないこと**
- 「タスクを抱えるとHPが減る」は直感的だが、**けんさんの負荷集中は本人の問題ではなく仕組みの問題**。それを個人のHP減少として表示すると、減点評価のように受け取られかねない
- 負荷の可視化は既に「負荷サマリー」が担っており、二重表現にもなる
- 採用するなら**加点方向に倒す**。例：タスク完了でEXPが貯まりLvが上がる／連続で締切を守るとバッジがつく。減る指標は置かない

**推奨:** MVPでは実装しない。P3で4人に配ってから、「もっと楽しくしたい」という声が出たら加点型で足す。

### 12.8 モックアップの位置づけ

共有されたモックアップは **全機能が実装された最終形** を描いたもの。
P0〜P3で作るのはこのうち「月カレンダー」「配信カードと出欠アイコン」「選択日の予定」「下部タブバー」だけ。
負荷ダッシュボード、タスク一覧、お知らせ履歴は後のフェーズで足す。**最初から全部を再現しようとしないこと。**

---

## 補足：既存ツールとの比較

正直に書いておくと、この要件の大部分は **Notion や Trello のテンプレートでも実現できる**。
それでも自作する価値があるのは、次の4点に絞られる。

1. **テンプレート自動生成と逆算締切** が完全にメモクラの工程に最適化されている（既製品だと毎回手動コピー）
2. **負荷の偏り警告** という、汎用ツールにはない「この課題専用」の機能を持てる
3. **専用アプリの通知**で、重要な連絡が埋もれない（既製ツールの通知は結局他アプリと混ざる）
4. **チームの世界観に合わせたUI**。メンバーが開きたくなること自体が機能になる
5. 開発者本人にとって、実ユーザー4名がいる **運用され続けるプロダクト** になる

逆に言えば、1と2を実装しないなら既製ツールを使ったほうがよい。P5・P6は削らないこと。
